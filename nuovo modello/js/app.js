const addons={essential:[['contact',60,'Modulo di contatto'],['whatsapp',30,'WhatsApp'],['maps',30,'Google Maps'],['gallery',80,'Galleria immagini'],['extra',90,'Pagina aggiuntiva'],['language',150,'Seconda lingua']],business:[['booking',120,'Calendario / prenotazioni'],['newsletter',80,'Newsletter'],['analytics',50,'Analytics'],['menu',100,'Menu / catalogo'],['quote',100,'Form preventivo avanzato'],['cms',200,'CMS / contenuti modificabili']],advanced:[['animations',150,'Animazioni avanzate'],['configurator',250,'Configuratore interattivo'],['payments',250,'Pagamenti online'],['login',300,'Login utenti'],['database',300,'Database'],['ai',300,'AI chatbot']]};let selected=new Map();let current='essential';
function renderAddons(){const el=document.querySelector('#addon-list');el.innerHTML='';addons[current].forEach(([id,price,label])=>{const row=document.createElement('div');row.className='addon';row.innerHTML=`<label><input type="checkbox" data-id="${id}" data-price="${price}" ${selected.has(id)?'checked':''}> <span>${label}</span></label><b>+ €${price}</b>`;el.appendChild(row)});el.querySelectorAll('input').forEach(i=>i.addEventListener('change',e=>{const {id,price}=e.target.dataset;e.target.checked?selected.set(id,+price):selected.delete(id);updateTotal()}))}
function updateTotal(){document.querySelector('#total').textContent='€'+(590+[...selected.values()].reduce((a,b)=>a+b,0))}
document.querySelectorAll('[data-tab]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('active'));b.classList.add('active');current=b.dataset.tab;renderAddons()}));
async function setLanguage(lang){try{const r=await fetch(`i18n/${lang}.json`);const t=await r.json();document.querySelectorAll('[data-i18n]').forEach(el=>{const v=el.dataset.i18n.split('.').reduce((o,k)=>o?.[k],t);if(v)el.innerHTML=v});document.documentElement.lang=lang;localStorage.setItem('lang',lang);document.querySelectorAll('[data-lang]').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang))}catch(e){console.warn('Translation unavailable',e)}}
document.querySelectorAll('[data-lang]').forEach(b=>b.addEventListener('click',()=>setLanguage(b.dataset.lang)));renderAddons();updateTotal();setLanguage(localStorage.getItem('lang')||'it');
